# Lucien2026

## Local database (PostgreSQL)
- `docker compose up -d`
- `npm run db:migrate`
- `npm run db:seed`
- `npm run db:import` (optional: import `dev.db`)

## Docs
- `docs/auth.md` (magic-link login setup)
- `docs/hr-supply-engine.md` (HR supply engine operator guide)
